spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iseries
    username: root
    password: root
    platform: mysql
    initialize: false
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

---
spring:
  profiles: container
  datasource:
    url: jdbc:mysql://mysql:3306/iseries
    username: root
    password: root
    initialize: true